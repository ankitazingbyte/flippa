<br><br><br>
<div class= "container">
  <center>
    <%- model_class = Domain -%>
    <div class="page-header">
      <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
    </div>
  </center>
  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= model_class.human_attribute_name(:name) %></th>
        <th><%= model_class.human_attribute_name(:price) %></th>
      </tr>
    </thead>
    <tbody>
      <% @domains.each do |domain| %>
        <tr> 
         
          <td><%= domain.name %></td>
          <td><%= domain.days %></td>
          <td><h5 class="text-right"> Price: <span style="color: green"><%= number_to_currency domain.price %></span></h5></td>
          <td>
            <%= link_to t('.show', :default => t("helpers.links.show")),
                        domain_path(domain), :class => 'btn btn-default btn-xs' %>
            <%= link_to t('.edit', :default => t("helpers.links.edit")),
                        edit_domain_path(domain), :class => 'btn btn-default btn-xs' %>
            <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                        domain_path(domain),
                        :method => :delete,
                        :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                        :class => 'btn btn-xs btn-danger' %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <center>
      <%= link_to 'NEW DOMAIN', new_domain_path, class: 'btn btn-primary' %>
  </center>
</div>
<br>